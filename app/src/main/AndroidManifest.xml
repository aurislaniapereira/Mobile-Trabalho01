<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufc.quixada.dadm.entrega01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Entrega01">
        <activity
            android:name=".Tela_Tabs"
            android:label="@string/title_activity_tela__tabs"
            android:theme="@style/Theme.Entrega01.NoActionBar"></activity>
        <activity android:name=".TelaGoToTab_GoToGrid" />
        <activity android:name=".Tela_Grid" />
        <activity android:name=".Lista" />
        <activity android:name=".DropdownList" />
        <activity android:name=".Som" />
        <activity android:name=".ToggleButton" />
        <activity android:name=".EditText" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>